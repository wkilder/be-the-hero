import { SpawnResult, SpawnOptions } from '@expo/spawn-async';
import bunyan from 'bunyan';
declare type Options = SpawnOptions & {
    logger?: bunyan;
    stdoutOnly?: boolean;
};
declare function spawn(command: string, args: string[], _options?: Options): Promise<SpawnResult>;
export default spawn;
