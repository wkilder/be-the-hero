"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const path_1 = __importDefault(require("path"));
const file_1 = require("./file");
const spawn_1 = __importDefault(require("./spawn"));
async function setup(ctx) {
    const { job, logger } = ctx;
    const buildDirectory = path_1.default.join(ctx.workingdir, 'build');
    const projectTarball = path_1.default.join(ctx.workingdir, 'project.tar.gz');
    logger.info({ projectUrl: job.projectUrl }, 'Download project archive');
    await file_1.fetchFile(job.projectUrl, projectTarball);
    logger.info('Unpacking project archive');
    await spawn_1.default('tar', ['--strip-components', '1', '-zxf', 'project.tar.gz', '-C', buildDirectory], { cwd: ctx.workingdir, logger });
    logger.info('Running yarn');
    await spawn_1.default('yarn', ['install'], { cwd: buildDirectory, logger });
    return buildDirectory;
}
exports.setup = setup;
//# sourceMappingURL=project.js.map