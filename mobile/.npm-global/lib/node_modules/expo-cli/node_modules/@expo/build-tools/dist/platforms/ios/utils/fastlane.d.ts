import { SpawnResult } from '@expo/spawn-async';
import bunyan from 'bunyan';
interface FastlaneOptions {
    logger?: bunyan;
    envs?: {
        [key: string]: string;
    };
    cwd?: string;
}
declare function runFastlane(fastlaneArgs: string[], { logger, envs, cwd }?: FastlaneOptions): Promise<SpawnResult>;
export default runFastlane;
