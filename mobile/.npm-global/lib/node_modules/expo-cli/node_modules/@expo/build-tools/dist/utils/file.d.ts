import { FileList } from '../types';
declare function readFileList(paths: string[], rootDir?: string): Promise<FileList>;
declare function fetchFile(srcUrl: string, outputPath: string, timeout?: number): Promise<void>;
export { fetchFile, readFileList, };
