"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const path_1 = __importDefault(require("path"));
const fs_extra_1 = __importDefault(require("fs-extra"));
const got_1 = __importDefault(require("got"));
const MAX_S3_FILE_FETCH_TIMOEUT = 1000 * 60 * 20;
async function readFileList(paths, rootDir = '.') {
    return Promise.all(paths.map(async (relativePath) => ({
        base64Data: await fs_extra_1.default.readFile(path_1.default.join(rootDir, relativePath), 'base64'),
        path: relativePath,
    })));
}
exports.readFileList = readFileList;
async function fetchFile(srcUrl, outputPath, timeout = MAX_S3_FILE_FETCH_TIMOEUT) {
    const writeStream = fs_extra_1.default.createWriteStream(outputPath);
    const stream = got_1.default.stream(srcUrl, { timeout });
    stream.pipe(writeStream);
    await new Promise((res, rej) => {
        stream.on('end', () => res());
        stream.on('error', (err) => rej(err));
    });
}
exports.fetchFile = fetchFile;
//# sourceMappingURL=file.js.map