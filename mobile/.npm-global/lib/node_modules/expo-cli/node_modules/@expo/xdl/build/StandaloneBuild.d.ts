import { Platform } from '@expo/config';
interface StandaloneBuildParams {
    id?: string;
    platform: Platform;
    limit: number;
    slug: string;
    owner?: string;
}
export declare function getStandaloneBuilds({ id, platform, limit, slug, owner, }: StandaloneBuildParams): Promise<any>;
export {};
